import { rad } from "app/math";

export const SLOPE_ANGLE = rad(12)
export const SLOPE_TAN = Math.tan(SLOPE_ANGLE)
export const SLOPE_LENGTH = 210
export const PLAYER_POSITION = -104
export const V = 5
